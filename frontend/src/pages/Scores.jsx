import { useEffect, useState } from "react";
import { useAuth } from "../contexts/AuthContext.jsx";
import { getResults } from "../services/dataService.js";
import PageHeader from "../components/PageHeader.jsx";
export default function Scores(){const{user}=useAuth();const[rows,setRows]=useState([]);useEffect(()=>{getResults().then(x=>setRows(x.filter(r=>r.studentId===user.uid)))},[user.uid]);return <><PageHeader eyebrow="Rekod prestasi" title="Markah saya" description="Semua keputusan kuiz disimpan bersama modul dan tarikh percubaan."/><div className="panel overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-white/5"><tr><th className="p-4">Modul</th><th className="p-4">Markah</th><th className="p-4">Betul</th><th className="p-4">Tarikh</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.quizId||i} className="border-t border-slate-100 dark:border-white/10"><td className="p-4 font-bold">{r.moduleCode}</td><td className="p-4"><span className="rounded-lg bg-tech-500/10 px-3 py-1 font-black text-tech-600">{r.score}%</span></td><td className="p-4">{r.correct??'-'} / {r.total}</td><td className="p-4 text-slate-500">{r.date?.seconds?new Date(r.date.seconds*1000).toLocaleString('ms-MY'):new Date(r.date).toLocaleString('ms-MY')}</td></tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-slate-500">Belum ada keputusan kuiz.</div>}</div></div></>}
