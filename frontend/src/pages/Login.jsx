import { useState } from "react";
import { Link, Navigate, useNavigate } from "react-router-dom";
import { useAuth } from "../contexts/AuthContext.jsx";
import Logo from "../components/Logo.jsx";
export default function Login(){const{user,login,firebaseEnabled}=useAuth();const nav=useNavigate();const[form,setForm]=useState({email:"student@demo.ikr",password:"demo123"});const[error,setError]=useState("");const[busy,setBusy]=useState(false);if(user)return <Navigate to={user.role==='lecturer'?'/lecturer':user.role==='admin'?'/admin':'/dashboard'} replace/>;async function submit(e){e.preventDefault();setBusy(true);setError("");try{const u=await login(form.email,form.password);nav(u?.role==='lecturer'?'/lecturer':u?.role==='admin'?'/admin':'/dashboard')}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="grid min-h-screen lg:grid-cols-[1.1fr_.9fr]"><section className="hidden bg-navy-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><Logo/><div><div className="eyebrow text-cyan-300">Satu platform · semua pembelajaran</div><h1 className="mt-4 max-w-xl text-5xl font-black leading-tight">Pembelajaran TVET rangkaian yang tersusun, interaktif dan boleh diukur.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-300">eModul, kuiz, AI Tutor, komunikasi kelas dan rekod prestasi dalam satu dashboard.</p></div><div className="text-sm text-slate-400">Program Teknologi Komputer Rangkaian (IKR)</div></section><section className="grid place-items-center bg-slate-50 p-5 dark:bg-navy-900"><div className="w-full max-w-md panel p-7 sm:p-9"><div className="mb-8 lg:hidden"><Logo darkText /></div><div className="eyebrow">Akses platform</div><h2 className="mt-2 text-3xl font-black">Log masuk</h2><p className="mt-2 text-sm text-slate-500">{firebaseEnabled?'Gunakan akaun Firebase anda.':'Gunakan akaun demo di bawah.'}</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-semibold">E-mel<input className="input mt-2" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label className="block text-sm font-semibold">Kata laluan<input className="input mt-2" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700 dark:bg-red-500/10 dark:text-red-300">{error}</div>}<button className="btn-primary w-full" disabled={busy}>{busy?'Memproses…':'Log masuk'}</button></form>{!firebaseEnabled&&<div className="mt-5 rounded-xl bg-blue-50 p-4 text-xs leading-6 text-blue-900 dark:bg-blue-500/10 dark:text-blue-200"><b>Akaun demo:</b><br/>student@demo.ikr · lecturer@demo.ikr · admin@demo.ikr<br/>Kata laluan: demo123</div>}<p className="mt-6 text-center text-sm text-slate-500">Belum mempunyai akaun? <Link className="font-bold text-tech-600" to="/register">Daftar</Link></p></div></section></div>}
