import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../contexts/AuthContext.jsx";
export default function Register(){const{register}=useAuth();const nav=useNavigate();const[form,setForm]=useState({role:'student',name:'',matricNo:'',staffId:'',className:'',email:'',password:''});const[error,setError]=useState('');async function submit(e){e.preventDefault();try{await register(form);nav(form.role==='lecturer'?'/lecturer':'/dashboard')}catch(err){setError(err.message)}}return <div className="grid min-h-screen place-items-center bg-slate-100 p-4 dark:bg-navy-950"><form onSubmit={submit} className="panel w-full max-w-xl p-7"><div className="eyebrow">Pendaftaran pengguna</div><h1 className="mt-2 text-3xl font-black">Cipta akaun</h1><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold sm:col-span-2">Role<select className="input mt-2" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="student">Pelajar</option><option value="lecturer">Pensyarah</option></select></label><label className="text-sm font-semibold sm:col-span-2">Nama penuh<input className="input mt-2" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label>{form.role==='student'?<><label className="text-sm font-semibold">No. matrik<input className="input mt-2" value={form.matricNo} onChange={e=>setForm({...form,matricNo:e.target.value})} required/></label><label className="text-sm font-semibold">Kelas<input className="input mt-2" value={form.className} onChange={e=>setForm({...form,className:e.target.value})} required/></label></>:<label className="text-sm font-semibold sm:col-span-2">ID staf<input className="input mt-2" value={form.staffId} onChange={e=>setForm({...form,staffId:e.target.value})} required/></label>}<label className="text-sm font-semibold">E-mel<input className="input mt-2" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label className="text-sm font-semibold">Kata laluan<input className="input mt-2" type="password" minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label></div>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-6 flex gap-3"><button className="btn-primary flex-1">Daftar</button><Link className="btn-secondary" to="/login">Kembali</Link></div></form></div>}
